{# {% extends 'base.html.twig' %}

{% block body_id 'searchTerms' %}

{% block main %}

    {{ component('searchTerms', {
        query: query
    }) }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %} #}